---
import { getImage } from 'astro:assets'

import heroDesktop from '../../assets/about/desktop/image-about-hero.jpg'
import heroMobile from '../../assets/about/mobile/image-about-hero.jpg'
import heroTablet from '../../assets/about/tablet/image-about-hero.jpg'

const heroMobileOpt = await getImage({ src: heroMobile, format: 'avif' })
const heroTabletOpt = await getImage({ src: heroTablet, format: 'avif' })
const heroDesktopOpt = await getImage({ src: heroDesktop, format: 'avif' })
---

<main>
  <div class="max-w-[1111px] mx-auto w-full md:px-10 xl:px-0 lg:grid lg:grid-cols-[635px_1fr]">
    <picture class="lg:order-2">
      <source
        media="(max-width: 767px)"
        srcset={heroMobileOpt.src}
        width={heroMobileOpt.attributes.width}
        height={heroMobileOpt.attributes.height}
      />
      <source
        media="(min-width: 1100px)"
        srcset={heroDesktopOpt.src}
        width={heroDesktopOpt.attributes.width}
        height={heroDesktopOpt.attributes.height}
      />
      <source
        media="(min-width: 768px)"
        srcset={heroTabletOpt.src}
        width={heroTabletOpt.attributes.width}
        height={heroTabletOpt.attributes.height}
      />
      <img
        src={heroDesktopOpt.src}
        alt=""
        width={heroDesktopOpt.attributes.width}
        height={heroDesktopOpt.attributes.height}
        class="w-full md:rounded-t-[15px] lg:rounded-l-none lg:rounded-r-[15px]"
      />
    </picture>
    <div
      class="h-[395px] md:h-[312px] lg:h-[480px] flex flex-col justify-center items-center text-white px-6 md:px-[58px] gap-6 md:gap-8 bg-peach md:rounded-b-[15px] lg:rounded-b-none lg:rounded-l-[15px] lg:items-start lg:pl-[95px]"
    >
      <h1 class="font-medium text-[32px] leading-[36px] md:text-[48px] md:leading-[48px]">
        About Us
      </h1>
      <p
        class="text-[15px] leading-[25px] text-center md:text-[16px] md:leading-[26px] lg:text-left lg:max-w-[458px]"
      >
        Founded in 2010, we are a creative agency that produces lasting results for our clients.
        We’ve partnered with many startups, corporations, and nonprofits alike to craft designs that
        make real impact. We’re always looking forward to creating brands, products, and digital
        experiences that connect with our clients’ audiences.
      </p>
    </div>
  </div>
</main>
